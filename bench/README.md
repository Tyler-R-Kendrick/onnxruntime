# Benchmark harness (planned)

This folder will host reproducible benchmark scripts and result schema checks for:
- stock ORT Web,
- custom runtime (compression off),
- custom runtime (compression on).

Current changeset initializes documentation and option plumbing needed for future benchmark execution.
